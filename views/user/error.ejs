<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error - MotionHaus</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
    }

    body {
      background: #fff;
      color: #000;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 40px 20px;
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
    }

    .error-code {
      font-size: 120px;
      font-weight: 800;
      color: #f0f0f0;
      margin-bottom: 20px;
    }

    .error-message {
      font-size: 24px;
      font-weight: 600;
      margin-bottom: 20px;
      color: #333;
    }

    .error-description {
      font-size: 16px;
      color: #666;
      margin-bottom: 40px;
      max-width: 600px;
    }

    .back-button {
      padding: 12px 24px;
      background: #000;
      color: #fff;
      border: none;
      border-radius: 4px;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      text-decoration: none;
    }

    .back-button:hover {
      background: #333;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="error-code"><%= error.status %></div>
    <h1 class="error-message"><%= message %></h1>
    <p class="error-description">
      <% if (error.status === 404) { %>
        The product you're looking for doesn't exist or has been removed.
      <% } else { %>
        Something went wrong while loading the product details. Please try again later.
      <% } %>
    </p>
    <a href="/products" class="back-button">Back to Products</a>
  </div>
</body>
</html> 