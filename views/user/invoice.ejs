<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice #<%= order.orderID %></title>
    <style>
        @page {
            size: A4;
            margin: 20px;
        }
        body {
            font-family: 'Courier New', monospace;
            margin: 0;
            padding: 20px;
            color: #000;
            font-size: 12px;
            line-height: 1.2;
            background: #fff;
        }
        .invoice-container {
            max-width: 100%;
            margin: 0 auto;
            padding: 20px;
        }
        .invoice-header {
            text-align: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid #000;
        }
        .company-info {
            margin-bottom: 20px;
        }
        .invoice-title {
            color: #000;
            font-size: 24px;
            margin: 0 0 10px 0;
            font-weight: bold;
        }
        .invoice-info {
            text-align: center;
            margin-bottom: 20px;
        }
        .invoice-details {
            margin: 20px 0;
        }
        .customer-info {
            margin-bottom: 20px;
            text-align: center;
        }
        .shipping-info {
            display: none;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
            font-family: 'Courier New', monospace;
        }
        th, td {
            padding: 8px 4px;
            text-align: left;
            border-bottom: 1px solid #000;
            font-size: 12px;
        }
        th {
            background-color: #f0f0f0;
            font-weight: bold;
            text-align: center;
        }
        .total-section {
            margin-top: 20px;
            text-align: right;
            font-family: 'Courier New', monospace;
        }
        .total-row {
            font-size: 14px;
            font-weight: bold;
        }
        .total-row p {
            margin: 5px 0;
            padding: 2px 0;
        }
        .footer {
            margin-top: 40px;
            text-align: center;
            color: #000;
            font-size: 12px;
            border-top: 1px solid #000;
            padding-top: 20px;
            font-family: 'Courier New', monospace;
        }
        h3 {
            margin: 0 0 10px 0;
            color: #000;
            font-size: 16px;
            text-align: center;
        }
        p {
            margin: 5px 0;
            text-align: center;
        }
        .status-badge {
            display: none;
        }
        .divider {
            border-top: 1px solid #000;
            margin: 10px 0;
        }
        .item-row {
            display: flex;
            justify-content: space-between;
            margin: 5px 0;
            font-family: 'Courier New', monospace;
        }
        .item-name {
            flex: 2;
        }
        .item-qty {
            flex: 1;
            text-align: center;
        }
        .item-price {
            flex: 1;
            text-align: right;
        }
        .item-total {
            flex: 1;
            text-align: right;
        }
    </style>
</head>
<body>
    <div class="invoice-container">
        <div class="invoice-header">
            <div class="company-info">
                <h1 class="invoice-title">MotionHaus (Logo)</h1>
            </div>
            <div class="invoice-info">
                <h2>Invoice</h2>
                <p><strong>Invoice #:</strong> <%= order.orderID %></p>
                <p><strong>Date:</strong> <%= new Date(order.orderDate).toLocaleDateString('en-GB', {day: '2-digit', month: 'short', year: 'numeric'}) %></p>
                <p><strong>Customer Email:</strong> <%= order.user.email %></p>
            </div>
        </div>

        <div class="divider"></div>

        <div class="invoice-details">
            <div class="item-row" style="font-weight: bold; border-bottom: 1px solid #000; padding-bottom: 5px;">
                <span class="item-name">Item</span>
                <span class="item-qty">Qty</span>
                <span class="item-price">Unit Price</span>
                <span class="item-total">Total</span>
            </div>
            
            <% order.items.forEach(item => { %>
                <div class="item-row">
                    <span class="item-name"><%= item.product.name %></span>
                    <span class="item-qty"><%= item.quantity %></span>
                    <span class="item-price">₹ <%= item.price.toFixed(2) %></span>
                    <span class="item-total">₹ <%= (item.price * item.quantity).toFixed(2) %></span>
                </div>
            <% }); %>
            
            <div class="divider"></div>

            <div class="total-section">
                <div class="total-row">
                    <p>Subtotal:                          ₹ <%= order.items.reduce((total, item) => total + (item.price * item.quantity), 0).toFixed(2) %></p>
                    <% if (order.coupon) { %>
                    <p>Coupon (<%= order.coupon.code %>):                -₹ <%= order.couponDiscount ? order.couponDiscount.toFixed(2) : '0.00' %></p>
                    <% } %>
                    <p>Shipping:                           Free</p>
                    <div class="divider"></div>
                    <p>TOTAL:                             ₹ <%= order.totalAmount.toFixed(2) %></p>
                </div>
            </div>
        </div>

        <div class="divider"></div>

        <div class="footer">
            <p>*This invoice includes all items in the order, regardless of their current status.</p>
            <p>This is a computer-generated invoice, no signature required.</p>
        </div>
    </div>
</body>
</html> 