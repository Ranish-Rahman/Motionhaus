<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>My Wallet - MotionHaus</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-dark: #1e1f22;
            --secondary-dark: #2d3748;
            --text-primary: #ffffff;
            --text-secondary: rgba(255, 255, 255, 0.8);
            --text-muted: rgba(255, 255, 255, 0.6);
            --border-color: rgba(255, 255, 255, 0.1);
            --sidebar-width: 250px;
        }

        body {
            margin: 0;
            font-family: 'Inter', sans-serif;
            background: linear-gradient(207deg, rgb(25, 25, 25), rgb(0, 0, 0));
            color: var(--text-primary);
            padding-top: 60px;
        }

        .layout {
            display: flex;
            min-height: calc(100vh - 60px);
        }

        .sidebar {
            width: var(--sidebar-width);
            background: var(--primary-dark);
            padding: 2rem 1.5rem;
            color: var(--text-primary);
            position: fixed;
            height: calc(100vh - 60px);
            left: 0;
            top: 60px;
            z-index: 10;
            border-right: 1px solid var(--border-color);
        }

        .sidebar-logo {
            font-size: 1.5rem;
            font-weight: bold;
            margin-bottom: 3rem;
            color: white;
            text-decoration: none;
            display: block;
        }

        .nav-link {
            color: var(--text-secondary);
            padding: 0.75rem 1rem;
            display: flex;
            align-items: center;
            gap: 1rem;
            text-decoration: none;
            transition: all 0.3s ease;
            border-radius: 8px;
        }

        .nav-link:hover,
        .nav-link.active {
            color: var(--text-primary);
            background: rgba(255, 255, 255, 0.1);
        }

        .nav-link i {
            font-size: 1.2rem;
        }

        .main-content {
            margin-left: var(--sidebar-width);
            flex-grow: 1;
            padding: 2rem;
            background: transparent;
        }

        .wallet-container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .wallet-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
        }

        .wallet-header h1 {
            color: var(--text-primary);
            font-size: 1.5rem;
            margin: 0;
        }

        .date-range {
            color: var(--text-muted);
            font-size: 0.9rem;
        }

        .balance-card {
            background: var(--primary-dark);
            border-radius: 16px;
            padding: 2rem;
            margin-bottom: 2rem;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.4);
            border: 1px solid var(--border-color);
        }

        .balance-label {
            font-size: 1.1rem;
            color: var(--text-muted);
            margin-bottom: 0.5rem;
        }

        .balance-amount {
            font-size: 2.5rem;
            font-weight: 600;
            color: var(--text-primary);
            margin-bottom: 1rem;
        }

        .card-info {
            color: var(--text-muted);
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .card-info i {
            color: #333;
        }

        .transactions-section {
            background: var(--primary-dark);
            border-radius: 16px;
            padding: 2rem;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.4);
            border: 1px solid var(--border-color);
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid var(--border-color);
        }

        .section-title {
            font-size: 1.2rem;
            font-weight: 600;
            color: var(--text-primary);
        }

        .transaction-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 0;
            border-bottom: 1px solid var(--border-color);
        }

        .transaction-item:last-child {
            border-bottom: none;
        }

        .transaction-details {
            display: flex;
            flex-direction: column;
            gap: 0.25rem;
        }

        .transaction-id {
            font-size: 0.9rem;
            color: var(--text-muted);
        }

        .transaction-amount {
            font-weight: 600;
            font-size: 1.1rem;
        }

        .transaction-amount.credit {
            color: #22c55e;
        }

        .transaction-amount.debit {
            color: #ef4444;
        }

        .transaction-date {
            color: var(--text-muted);
            font-size: 0.9rem;
            text-align: right;
        }

        .status {
            padding: 0.25rem 0.75rem;
            border-radius: 50px;
            font-size: 0.8rem;
            margin-top: 0.5rem;
            display: inline-block;
        }

        .status-success {
            background-color: rgba(34, 197, 94, 0.2);
            color: #22c55e;
        }

        .status-pending {
            background-color: rgba(234, 179, 8, 0.2);
            color: #eab308;
        }

        .status-cancel {
            background-color: rgba(239, 68, 68, 0.2);
            color: #ef4444;
        }

        .no-transactions {
            text-align: center;
            padding: 32px;
            color: var(--text-muted);
        }

        /* Pagination Styles */
        .pagination-container {
            display: flex;
            justify-content: center;
            margin-top: 2rem;
            padding: 1rem;
        }

        .pagination {
            display: flex;
            gap: 0.5rem;
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .page-item {
            display: inline-block;
        }

        .page-link {
            display: flex;
            align-items: center;
            justify-content: center;
            min-width: 40px;
            height: 40px;
            padding: 0.5rem;
            border: 1px solid var(--border-color);
            border-radius: 4px;
            color: var(--text-secondary);
            text-decoration: none;
            transition: all 0.2s ease;
            background: var(--primary-dark);
        }

        .page-link:hover {
            background: rgba(255, 255, 255, 0.1);
            border-color: var(--text-secondary);
            color: var(--text-primary);
        }

        .page-item.active .page-link {
            background: var(--text-primary);
            border-color: var(--text-primary);
            color: var(--primary-dark);
        }

        .page-item.disabled .page-link {
            color: var(--text-muted);
            pointer-events: none;
            background: var(--secondary-dark);
            border-color: var(--border-color);
        }

        .page-info {
            text-align: center;
            color: var(--text-muted);
            margin-top: 1rem;
            font-size: 0.875rem;
        }

        @media (max-width: 768px) {
            .layout {
                flex-direction: column;
            }

            .sidebar {
                width: 100%;
                height: auto;
                position: relative;
                top: 0;
            }

            .main-content {
                margin-left: 0;
                padding: 1rem;
            }

            .wallet-container {
                padding: 0 10px;
            }

            /* Mobile pagination improvements */
            .pagination {
                gap: 0.25rem;
            }

            .page-link {
                min-width: 35px;
                height: 35px;
                font-size: 0.875rem;
            }

            .page-info {
                font-size: 0.8rem;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar') %>
    <div class="layout">
        <%- include('../partials/user-profile-sidebar', { currentPage: 'wallet' }) %>
        
        <main class="main-content">
            <div class="wallet-container">
                <div class="wallet-header">
                    <h1>My Wallet</h1>
                </div>

                <div class="balance-card">
                    <div class="balance-label">Available Balance</div>
                    <div class="balance-amount">₹<%= typeof user.wallet === 'number' ? user.wallet.toFixed(2) : (user.wallet?.balance || 0).toFixed(2) %></div>
                    <div class="card-info">
                        <i class="bi bi-credit-card"></i>
                        <span>Use your wallet for payments</span>
                    </div>
                </div>

                <div class="transactions-section">
                    <div class="section-header">
                        <div class="section-title">
                            <% if (totalPages > 1) { %>
                                Transactions (Page <%= pageNumber %> of <%= totalPages %>)
                            <% } else { %>
                                Recent Transactions
                            <% } %>
                        </div>
                        <% if (totalPages > 1) { %>
                            <div class="text-muted" style="font-size: 0.9rem;">
                                Total: <%= totalTransactions %> transactions
                            </div>
                        <% } %>
                    </div>

                    <% if (transactions && transactions.length > 0) { %>
                        <div class="transaction-list">
                            <% transactions.forEach(transaction => { %>
                                <div class="transaction-item">
                                    <div class="transaction-details">
                                        <div class="transaction-id"><%= transaction.description %></div>
                                        <div class="transaction-amount <%= transaction.type %>">
                                            <%= transaction.type === 'credit' ? '+' : '-' %>₹<%= transaction.amount.toFixed(2) %>
                                        </div>
                                    </div>
                                    <div class="transaction-info">
                                        <div class="transaction-date">
                                            <%= new Date(transaction.createdAt).toLocaleDateString('en-US', { 
                                                day: 'numeric',
                                                month: 'short',
                                                year: 'numeric'
                                            }) %>
                                        </div>
                                        <% 
                                            let statusClass = '';
                                            if (transaction.status === 'success') statusClass = 'status-success';
                                            else if (transaction.status === 'pending') statusClass = 'status-pending';
                                            else if (transaction.status === 'cancel') statusClass = 'status-cancel';
                                        %>
                                        <div class="status <%= statusClass %>">
                                            <%= transaction.status ? transaction.status.charAt(0).toUpperCase() + transaction.status.slice(1) : 'Success' %>
                                        </div>
                                    </div>
                                </div>
                            <% }) %>
                        </div>
                    <% } else { %>
                        <div class="no-transactions">
                            <p>No transactions yet</p>
                        </div>
                    <% } %>

                    <!-- Pagination -->
                    <% if (transactions && transactions.length > 0 && totalPages > 1) { %>
                        <div class="pagination-container">
                            <nav aria-label="Page navigation">
                                <ul class="pagination">
                                    <!-- Previous Page -->
                                    <li class="page-item <%= pageNumber === 1 ? 'disabled' : '' %>">
                                        <a class="page-link" href="?page=<%= pageNumber - 1 %>" aria-label="Previous">
                                            <span aria-hidden="true">&laquo;</span>
                                        </a>
                                    </li>

                                    <!-- Page Numbers -->
                                    <% for(let i = 1; i <= totalPages; i++) { %>
                                        <li class="page-item <%= pageNumber === i ? 'active' : '' %>">
                                            <a class="page-link" href="?page=<%= i %>"><%= i %></a>
                                        </li>
                                    <% } %>

                                    <!-- Next Page -->
                                    <li class="page-item <%= pageNumber === totalPages ? 'disabled' : '' %>">
                                        <a class="page-link" href="?page=<%= pageNumber + 1 %>" aria-label="Next">
                                            <span aria-hidden="true">&raquo;</span>
                                        </a>
                                    </li>
                                </ul>
                            </nav>
                        </div>
                        <div class="page-info">
                            Showing <%= (pageNumber - 1) * limit + 1 %> to <%= Math.min(pageNumber * limit, totalTransactions) %> of <%= totalTransactions %> transactions
                        </div>
                    <% } %>
                </div>
            </div>
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
